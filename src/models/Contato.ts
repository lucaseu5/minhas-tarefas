class Contato {
  nome: string
  celular: string
  mail: string
  id: number

  constructor(nome: string, celular: string, mail: string, id: number) {
    this.nome = nome
    this.celular = celular
    this.mail = mail
    this.id = id
  }
}

export default Contato
